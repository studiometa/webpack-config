{% extends '@pkg-layouts/default.twig' %}

{% block footer %}
  {# <h2>Navigation</h2> #}
  <ul>
    {% for page in pages().all() %}
      <li><a href="{{ page.href }}">{{ page.href }}</a></li>
    {% endfor %}
  </ul>

  {{ dump(page()) }}
{% endblock %}
